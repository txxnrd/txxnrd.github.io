export declare class Poller {
    readonly action: () => Promise<void>;
    private readonly ms;
    private poller;
    constructor(action: () => Promise<void>, ms: number);
    start(): void;
    stop(): void;
}
