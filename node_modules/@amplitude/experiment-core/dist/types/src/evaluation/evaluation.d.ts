import { EvaluationCondition, EvaluationFlag, EvaluationVariant } from './flag';
type EvaluationTarget = {
    context: Record<string, unknown>;
    result: Record<string, EvaluationVariant>;
};
export declare class EvaluationEngine {
    evaluate(context: Record<string, unknown>, flags: EvaluationFlag[]): Record<string, EvaluationVariant>;
    private evaluateFlag;
    private evaluateSegment;
    evaluateConditions(target: EvaluationTarget, conditions: EvaluationCondition[][]): boolean;
    private matchCondition;
    private getHash;
    private bucket;
    private matchNull;
    private matchSet;
    private matchString;
    private matchesIs;
    private matchesContains;
    private matchesComparable;
    private comparator;
    private versionComparator;
    private matchesRegex;
    private containsNone;
    private containsBooleans;
    private parseNumber;
    private coerceString;
    private coerceStringArray;
    private isSetOperator;
    private setEquals;
    private matchesSetContainsAll;
    private matchesSetContainsAny;
}
export {};
